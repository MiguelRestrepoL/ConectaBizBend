trigger:
- produccion

pool:
  name: Default

steps:
- script: |
    "C:\Program Files\nodejs\node.exe" --version
  displayName: 'Verificar Node.js'

- script: |
    "C:\Program Files\nodejs\npm.cmd" install
  displayName: 'Instalar dependencias'

- script: |
    "C:\Program Files\nodejs\npm.cmd" test
  displayName: 'Correr tests'